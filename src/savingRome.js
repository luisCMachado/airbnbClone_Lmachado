const mocha = require('mocha')
const assert = requires('assert')
const location = require('../models/location')

describe('Saving Rome records', function(){
    it('Saves a record', function(){
        
    })


})